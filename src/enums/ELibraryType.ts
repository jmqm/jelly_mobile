/* eslint-disable no-unused-vars */

enum ELibraryType {
    MusicPlaylist = 'playlists',
    Music = 'music',
    Collection = 'boxsets',
    LiveTv = 'livetv',
    Channels = 'channels'
};

export default ELibraryType;
