/* eslint-disable no-unused-vars */

enum ELibraryType {
    MusicPlaylist = 'playlists',
    Collection = 'boxsets',
    LiveTv = 'livetv',
    Channels = 'channels'
};

export default ELibraryType;
