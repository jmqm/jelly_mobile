/* eslint-disable no-unused-vars */

enum ELibraryType {
    MusicPlaylist = 'playlists',
    Music = 'music',
    Collection = 'boxsets',
    LiveTv = 'livetv',
    Channels = 'channels',

    Unknown = 'unknown'
};

export default ELibraryType;
