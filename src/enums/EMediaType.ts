/* eslint-disable no-unused-vars */

enum EMediaType {
    Movie = 'Movie',
    Series = 'Series',
    Episode = 'Episode',
    Audio = 'Audio',
    CollectionFolder = 'CollectionFolder'
};

export default EMediaType;
